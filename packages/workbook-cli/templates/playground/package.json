{
  "name": "%%SLUG%%",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "workbook dev",
    "build": "workbook build",
    "check": "workbook check"
  },
  "dependencies": {
    "@work.books/runtime": "^0.7.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@work.books/cli": "%%CLI_VERSION%%",
    "svelte": "^5.0.0"
  }
}
